# depix

Generate PNGs for iOS and Android from SVGs.

## Dependencies

 * `librsvg`
   * `libpng`
   * `freetype`
   * `gettext`
   * `glib`
   * `cairo`
   * `gobject-introspection`
   * `atk`
   * `icu4c`
   * `harfbuzz`
   * `gtk+`
   * `libcroco`
